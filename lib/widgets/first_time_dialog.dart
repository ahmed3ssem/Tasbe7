import 'package:flutter/material.dart';

class FirstTimeDialog extends StatelessWidget {
  const FirstTimeDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
